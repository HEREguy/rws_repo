[project]
name = "rj_app"
version = "0.1.0"
description = "Default template for PDM package"

dependencies = ["dbt-core>=1.9.2"]
requires-python = "==3.13.*"
readme = "README.md"

[tool.pdm]
distribution = false
